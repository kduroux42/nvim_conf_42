# nvim_conf_42
Add nvim for 42 Mac/DEL/n
get nvim appimage, extract and move in $HOME/n
move nvim in .config /n
and do alias/n
### Install :
```
bash -c "$(curl -fsSL https://raw.github.com/kduroux42/nvim_conf_42/master/install.sh)"
```
